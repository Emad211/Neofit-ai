name: Stage NeoFit Nutrition RC Payload

on:
  pull_request:
    branches:
      - agent/iranian-food-kb-foundation
    paths:
      - '.github/workflows/apply-neofit-nutrition-rc.yml'

permissions:
  contents: read

jobs:
  payload-staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Direct text payload is being staged; no product mutation occurs in this run.'
