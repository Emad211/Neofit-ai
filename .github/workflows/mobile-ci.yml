name: Mobile CI

on:
  push:
    branches:
      - agent/mobile-local-first-expo
  pull_request:
    branches:
      - master

permissions:
  contents: read

concurrency:
  group: mobile-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-mobile:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: mobile
    env:
      CI: '1'
      EXPO_NO_TELEMETRY: '1'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.1

      - name: Install mobile dependencies
        run: npm install --no-audit --no-fund

      - name: Upload generated lockfile
        uses: actions/upload-artifact@v4
        with:
          name: mobile-package-lock
          path: mobile/package-lock.json
          if-no-files-found: error
          retention-days: 3

      - name: Check Expo package versions
        run: npx expo install --check

      - name: Capture Expo Doctor diagnostics
        id: doctor
        shell: bash
        run: |
          set +e
          npm run doctor > expo-doctor.log 2>&1
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload Expo Doctor diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: expo-doctor-diagnostics
          path: mobile/expo-doctor.log
          if-no-files-found: error
          retention-days: 3

      - name: Capture TypeScript diagnostics
        id: typecheck
        shell: bash
        run: |
          set +e
          npm run typecheck > typecheck.log 2>&1
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload TypeScript diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: typescript-diagnostics
          path: mobile/typecheck.log
          if-no-files-found: error
          retention-days: 3

      - name: Export Android bundle
        run: npx expo export --platform android --output-dir dist

      - name: Enforce validation results
        if: steps.doctor.outputs.exit_code != '0' || steps.typecheck.outputs.exit_code != '0'
        run: |
          echo "Expo Doctor exit code: ${{ steps.doctor.outputs.exit_code }}"
          echo "TypeScript exit code: ${{ steps.typecheck.outputs.exit_code }}"
          exit 1
