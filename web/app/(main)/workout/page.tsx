import { RoutePlaceholder } from '@/components/route-placeholder';

export default function WorkoutPage() {
  return <RoutePlaceholder title="تمرین" icon="workout" description="پوسته و مسیر آماده است؛ صفحهٔ کامل تمرین در برش بعدی از فرانت تأییدشده منتقل می‌شود." />;
}
